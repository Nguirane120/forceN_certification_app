import 'package:flutter/material.dart';

class Favorte extends StatelessWidget {
  const Favorte({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Favorte'),
    );
  }
}
